name: Build ArtMusic

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest
    
    steps:
      - uses: actions/checkout@v4
      
      # Node setup
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
      
      # Rust setup
      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable
        
      - name: Rust cache
        uses: swatinem/rust-cache@v2
        with:
          workspaces: './src-tauri -> target'
      
      # Install dependencies
      - name: Install npm dependencies
        run: npm install
      
      # Debug node version
      - name: Debug info
        run: node --version && npm --version
        
      # List node_modules
      - name: List modules
        run: ls node_modules | head -20
      
      # Build frontend
      - name: Build frontend
        run: |
          npm run build 2>&1
          echo "Exit code: $?"
      
      # Build Tauri (without release - just build .exe)
      - name: Build Tauri app
        run: cd src-tauri && cargo build --release
        
      # Upload .exe as artifact
      - name: Upload exe
        uses: actions/upload-artifact@v4
        with:
          name: artmusic-windows
          path: src-tauri/target/release/*.exe
          if-no-files-found: error
